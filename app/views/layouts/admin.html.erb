<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin - Music Theory" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-sans leading-relaxed text-gray-900 bg-gray-100">
    <nav class="bg-gray-900 text-white py-4 sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-6 flex flex-col md:flex-row justify-between items-center gap-4">
        <%= link_to "🔧 Admin Panel", admin_root_path, class: "text-xl font-bold text-white no-underline" %>
        <div class="flex flex-wrap justify-center gap-6 items-center">
          <%= link_to "Dashboard", admin_root_path, class: "text-gray-300 no-underline font-medium hover:text-white transition-colors" %>
          <%= link_to "Users", admin_users_path, class: "text-gray-300 no-underline font-medium hover:text-white transition-colors" %>
          <%= link_to "Quiz Attempts", admin_quiz_attempts_path, class: "text-gray-300 no-underline font-medium hover:text-white transition-colors" %>
          <span class="text-gray-500">|</span>
          <%= link_to "← Back to App", root_path, class: "text-gray-300 no-underline font-medium hover:text-white transition-colors" %>
          <%= button_to "Sign Out", session_path, method: :delete, class: "inline-flex items-center justify-center px-4 py-2 text-sm font-medium rounded-lg border border-gray-600 text-gray-300 bg-transparent hover:bg-gray-800 transition-all cursor-pointer" %>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto px-6 py-8">
      <% if notice.present? %>
        <div class="p-4 rounded-lg mb-6 bg-green-100 text-green-800 border border-green-200"><%= notice %></div>
      <% end %>
      <% if alert.present? %>
        <div class="p-4 rounded-lg mb-6 bg-red-100 text-red-800 border border-red-200"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>

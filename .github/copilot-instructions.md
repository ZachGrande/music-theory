# Copilot Instructions

## Data Format Preferences

- **Always prefer YAML over JSON** when writing data files, configuration files, or serialized data.
- Use YAML for fixtures, seed data, configuration, and any structured data storage.
